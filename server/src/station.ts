/**
 * The operator's mode and the threshold it needs — see `shared/modes.ts`.
 *
 * The table was written out here and again twice in the app, with a test
 * that read the app's source with a regular expression to check the three
 * agreed. One module now, and the check is the compiler.
 */
export * from '../../shared/modes.ts';
