/**
 * The coverage grid and its reach threshold — see `shared/coverageGrid.ts`.
 *
 * The same re-export the app has in `mobile/src/data/coverageGrid.ts`, so
 * one set of numbers decides what both projects draw.
 */
export * from '../../shared/coverageGrid.ts';
