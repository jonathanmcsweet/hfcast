{
  "name": "hfcast-server",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "VOACAP prediction and space weather API for the HFcast app",
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "start": "node --experimental-strip-types src/index.ts",
    "dev": "node --experimental-strip-types --watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "record-fixture": "node --experimental-strip-types test/recordFixture.ts",
    "test": "node --experimental-strip-types --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "~7.0.2"
  }
}
