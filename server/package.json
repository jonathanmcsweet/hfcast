{
  "name": "hfcast-server",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "VOACAP prediction and space weather API for the HFcast app",
  "scripts": {
    "start": "node --experimental-strip-types src/index.ts",
    "dev": "node --experimental-strip-types --watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "~5.6.3"
  }
}
