# The flat-layout setting this project needs now lives in pnpm-workspace.yaml
# beside this file, as `nodeLinker: hoisted`, with the reasoning. pnpm 11 no
# longer reads pnpm settings from .npmrc, so putting it back here would look
# correct and do nothing.
