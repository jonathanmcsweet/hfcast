/**
 * The correction applied to a coverage map — see `shared/correctMap.ts`.
 *
 * The same arithmetic the band table gets, so the two screens cannot
 * describe one place two ways.
 */
export * from '../../../shared/correctMap.ts';
